---
description: "A Theme component."
labels: ["label1", "label2", "label3"]
---

import { Theme } from "./theme";
import { Button } from "@matheuschignolli/example.dumb.button";

## React Component for rendering text

A basic div that renders some text

### Component usage

```js
<Theme text="hello from Theme" />
```

### Using props to customize the text

Modify the text to see it change live:

```js live
<Theme theme={{ palette: { primary: { main: "#FF0" } } }}>
  <Button>hello from Theme</Button>
</Theme>
```
